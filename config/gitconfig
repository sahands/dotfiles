[user]
    name = Sahand Saba
    email = sahands@gmail.com
    signingkey = 2B8C396C

; [push]
;     default = simple
;
; [merge]
;     tool = threesome
;
; [mergetool "threesome"]
;     cmd = "mvim -f $BASE $LOCAL $REMOTE $MERGED -c 'ThreesomeInit'"
;     trustExitCode = true
